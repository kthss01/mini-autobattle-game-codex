import Phaser from 'phaser';
import { GameConfig } from './game/GameConfig.js';

// Placeholder bootstrap for ticket 1.
// Rendering integration with sim is planned from ticket 6.
new Phaser.Game(GameConfig);
